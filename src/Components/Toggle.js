import { useState } from "react";
import './Toggle.css'

function Toggle () {
    const [mode, setMode] = useState("false")
    return (
        <div>
            <h1>GOOGLE</h1>
        </div>
    )
}